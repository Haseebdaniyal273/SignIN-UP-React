posts.map(post => (
    if ( username == post.username){
      alert("Working")
    }
  
  ))

  posts.map(post => (
    <div>
  {
  (username = post.name){
  
  }else{
  
  }}
    </div>
   ))


   const next = ()=>{
    if(username == posts.map(post => (post.username ) ) )
    {
      navigate("/")
    }else{
      alert("Not working")
    }
    
    }